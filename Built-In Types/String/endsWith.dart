/// In Dart, the `String` class provides the `endsWith` methods, which is used to check if a 
/// string ends with a specific substring. This method is
/// particularly useful fo rvalidataion and pattern matching where the suffix of a string is
/// important. 

void main() {
  String text = "Hello World";
  var value = text.endsWith("l");
  print(value);
}
